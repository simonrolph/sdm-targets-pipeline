---
title: "Preparation for running models"
author: "Simon Rolph"
date: "2023-01-20"
output: html_document
---


```{r}
#library(terra)
#library(sf)
```

Load data


```{r}
#occ_data <- readRDS("data/derived/occurence/5334220.rds")
#env_data <- rast("data/derived/environmental/env-layers-OSGB.tif")
```

Generate background samples and extract environmental data for presence occurrence data


```{r}
generate_samples <- function(species,n_background,env_data,occ_data){
  set.seed(42)
  data_background <- spatSample(env_data,n_background,xy=TRUE,method =  "random")
  data_background$pres <- 0
  data_background$ID <- NA
  
  data_background$fold <- rep_len(1:10,n_background)
  
  data_presence <- extract(env_data,occ_data,xy=TRUE)
  data_presence$pres <- 1
  data_presence$fold <- rep_len(1:10,nrow(data_presence))
  
  data_full <- rbind(data_background,data_presence)
  data_full
}

#saveRDS(data_full,"data/derived/occurence/5334220_presence_absence.RDS")
```




